{
  "name": "prowl",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm db:migrate && pnpm --parallel --filter @prowl/web --filter @prowl/worker dev",
    "dev:web": "pnpm --filter @prowl/web dev",
    "dev:worker": "pnpm --filter @prowl/worker dev",
    "build": "pnpm --filter @prowl/web build",
    "db:generate": "pnpm --filter @prowl/db generate",
    "db:migrate": "pnpm --filter @prowl/db migrate",
    "typecheck": "pnpm -r typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:golden": "cross-env PROWL_GOLDEN=1 vitest run --config vitest.golden.config.ts",
    "lint": "eslint .",
    "seed:demo": "cross-env PROWL_DATA_DIR=./data-demo tsx --env-file-if-exists=.env scripts/seed-demo.ts",
    "bootstrap": "node scripts/setup.mjs",
    "dev:demo": "cross-env PROWL_DATA_DIR=./data-demo pnpm dev"
  },
  "devDependencies": {
    "@prowl/core": "workspace:*",
    "@prowl/db": "workspace:*",
    "@prowl/documents": "workspace:*",
    "@prowl/shared": "workspace:*",
    "@types/node": "^24.0.0",
    "cross-env": "^10.1.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
